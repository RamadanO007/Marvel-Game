package model.world;

/**
 * An enum representing the different possible moving/ casting directions.
 * Possible values are: RIGHT, LEFT, UP, and DOWN.
 */
public enum Direction {
    RIGHT, LEFT, UP, DOWN;
}
